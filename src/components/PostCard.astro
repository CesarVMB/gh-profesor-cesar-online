---
import { getPostImage } from '../utils/getPostImage';
import { categoryLabels } from '../utils/categories';

const { post } = Astro.props;

// URLs
const postUrl = `/publicaciones/${post.slug}/`;
const categoryUrl = `/categoria/${post.data.category}/1`;

// Imagen con fallback (YouTube → image → default)
const imageSrc = getPostImage(post);

// Fecha formateada
const formattedDate = post.data.pubDate.toLocaleDateString('es-PE', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
---
<div class="card h-100 shadow-sm post-card">

  <!-- 🖼 Imagen -->
  <img
    src={imageSrc}
    class="card-img-top"
    alt={post.data.title}
    loading="lazy"
  />

  <div class="card-body d-flex flex-column">

    <!-- 📂 Categoría + 📅 Fecha -->
    <div class="mb-2 d-flex justify-content-between align-items-center">
      <a
        href={categoryUrl}
        class="badge bg-secondary text-decoration-none"
        aria-label={`Ver categoría ${categoryLabels[post.data.category]}`}
      >
        {categoryLabels[post.data.category] ?? post.data.category}
      </a>

      <small class="text-muted">
        {formattedDate}
      </small>
    </div>

    <!-- 📝 Título -->
    <h5 class="card-title">
      {post.data.title}
    </h5>

    <!-- 📄 Descripción -->
    <p class="card-text text-muted">
      {post.data.description}
    </p>

    <!-- 🏷️ Etiquetas -->
    {post.data.tags && post.data.tags.length > 0 && (
      <div class="mb-3">
        {post.data.tags.map(tag => (
          <a
            href={`/tags/${tag}/1`}
            class="badge bg-light text-dark border me-1 text-decoration-none"
            aria-label={`Filtrar por etiqueta ${tag}`}
          >
            #{tag}
          </a>
        ))}
      </div>
    )}

    <!-- 🔗 Botón -->
    <div class="mt-auto">
      <a
        href={postUrl}
        class="btn btn-primary btn-sm"
        aria-label={`Leer más sobre ${post.data.title}`}
      >
        Leer más
      </a>
    </div>

  </div>
</div>

<style>
.card-img-top {
  aspect-ratio: 16/9;
  object-fit: cover;
  width: 100%;
  height: 100%;
}
</style>
