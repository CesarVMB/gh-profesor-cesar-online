---
import { getYouTubeId } from '../utils/youtube';

const { youtube, alt } = Astro.props;
const videoId = getYouTubeId(youtube);
---

{videoId && (
  <img
    src={`https://img.youtube.com/vi/${videoId}/hqdefault.jpg`}
    class="card-img-top"
    alt={alt}
    loading="lazy"
  />
)}

