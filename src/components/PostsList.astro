---
import PostCard from './PostCard.astro';
const { posts } = Astro.props;
---
<div class="row g-4">
  {posts.map(post => (
  <div class="col-md-4">
    <PostCard post={post} />
  </div>
  ))}
</div>

